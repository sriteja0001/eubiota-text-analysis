# Eubiota Text Analysis

Public analysis scaffold for the Eubiota human text-evaluation data.

## Private data

The local `data/` folder is intentionally excluded from this repository because it contains private study data.

The repository `.gitignore` includes:

```gitignore
data/
```

Place private CSVs locally under `data/` when running analysis code, but do not commit them.
